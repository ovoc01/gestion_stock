version: '3.8'

services:
  frontend:
    build: 
      context: .
      dockerfile: frontend/Dockerfile
    ports:
      - "3000:80"

  backend:
    build:
      context: .
      dockerfile: backend/Dockerfile
    ports:
      - "8080:8080"
  

  ml_service:
    build:
      context: .
      dockerfile: ml_service/Dockerfile
    ports:
      - "5000:5000"